import type {Metadata} from "next";
import {PrimalsGuide} from "@/components/guides/primals-guide";

export const metadata:Metadata={title:"Primals — Soutenroku",description:"Account-focused Optimus transition guidance, beginning with single-sided and double-sided Water Varuna grids."};

export default function Page(){return <PrimalsGuide/>}
