import type {Metadata} from "next";
import {ExchangePage} from "@/components/reference/exchange-page";

export const metadata:Metadata={title:"Exchange Priorities — Soutenroku",description:"Account-specific Renown, Prestige, Daily Point, Valor Badge, certificate, and drawbox priorities."};
export default function Page(){return <ExchangePage/>}
